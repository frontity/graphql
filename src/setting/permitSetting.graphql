query permitSetting($input_packageId: ID!, $input_siteId: ID!) {
  SomeSiteExists(
    filter: {
      AND: [
        {
          id: $input_siteId
          settings_none: { package: { id: $input_packageId } }
        }
      ]
    }
  )
}
